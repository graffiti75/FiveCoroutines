<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.cericatto.sevencoroutines">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SevenCoroutines">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Exemplo1Activity"/>
        <activity android:name=".Exemplo2Activity"/>
        <activity android:name=".Exemplo3Activity"/>
        <activity android:name=".Exemplo4Activity"/>
        <activity android:name=".Exemplo5Activity"/>
    </application>
</manifest>